{% extends 'base.html.twig' %}

{% block title %}Edit Logements{% endblock %}

{% block body %}
    <div class="navbar">{% include ('/navbar/index.html.twig') %}</div>
    <div class="container">
        <h1>Edit Logements</h1>
        <div class="col-md-12"> {# Utilisez col-md-6 pour diviser la rangée en deux colonnes #}
            {{ include('logements/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="col-md-6"> {# Deuxième colonne #}
            {{ include('logements/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
