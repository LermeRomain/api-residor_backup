<form method="post" action="{{ path('app_logements_delete', {'id': logement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logement.id) }}">
    <button class="btn btn-danger mt-3 mb-3">Supprimer le logement</button>
</form>
